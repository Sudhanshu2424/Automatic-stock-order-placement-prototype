# Automatic-stock-order-placement-prototype
Used Alice blue API to fetch stock market data and then used python algorithms to apply conditions over it and placing hundreds of stock orders simultaneously and automatically notifying it on telegram.
